<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <%= link_to "Sophie Bonneval", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <% if logged_in? %>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Articles
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <%= link_to "All articles", articles_path, class: "nav-link" %>
                <%= link_to "Articles by Category", categories_path, class: "nav-link" %>
                <div class="dropdown-divider"></div>
                <%= link_to "New article", new_article_path, class: "nav-link" %>
                <% if logged_in? && current_user.admin? %>
                <%= link_to "New category", new_category_path, class: "nav-link" %>
                <% end %>
              </div>
            </li>
        <% else %>
            <li class="nav-item">
                <%= link_to "Articles", articles_path, class: "nav-link" %>
            </li>
        <% end %>
        <li class="nav-item">
            <%= link_to "Categories", categories_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
            <%= link_to "About", about_path, class: "nav-link" %>
        </li>
      </ul>
      <form class="form-inline my-2 my-lg-0">
        <ul class="navbar-nav mr-auto">
            <% if logged_in? %>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <%= "(Admin)" if current_user.admin? %> <%= current_user.username %>
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <%= link_to "View your profile", user_path(current_user), class: "nav-link" %>
                        <%= link_to "Edit your profile", edit_user_path(current_user), class: "nav-link" %>
                    </div>
                </li>
                <li class="nav-item">
                    <a class="nav-link">-</a>
                </li>
                <li class="nav-item">
                    <%= link_to "Log out", logout_path, class: "nav-link", method: :delete %>
                </li>
            <% else %>
                <li class="nav-item ml-3">
                    <%= link_to "Github", "https://github.com/Maldorana", class: "btn btn-primary" %>
                </li>
            <% end %>
        </ul>
      </form>
    </div>
</nav>
